import styled from 'styled-components'

const StyledDirections = styled.div`
  > p {
    color: #fff;
    font-size: 20px;
    padding: 0 10px;
    font-weight: bold;
    border-radius: 8px;
    background: #e4163d;
    display: inline-block;
  }
`

export default StyledDirections