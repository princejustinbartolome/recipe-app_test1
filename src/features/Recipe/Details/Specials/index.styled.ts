import styled from 'styled-components'

const StyledSpecials = styled.ul`
  .ttlBtnContainer {
    overflow: hidden;
    label {
      float: left;
      width: initial;
    }
    button {
      border: none;
      float: right;
      width: initial;
      background: none;
      svg {
        margin-bottom: 3px;
      }
    }
  }
`

export default StyledSpecials